import { Time } from "@angular/common";

export class CalendarEvent {
  id: number;
  // patient_id: string;
  // patient_name: string;
  // physician_id: string;
  // physician_name: string;
  title: string;
  description: string;
  date: string;
  // dummy:string
  // time: string;
  // status: string;
}
